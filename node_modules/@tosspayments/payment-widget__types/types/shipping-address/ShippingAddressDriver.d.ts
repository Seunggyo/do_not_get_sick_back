import { ShippingAddressWidget } from './ShippingAddressWidget';
import { ShippingAddressClient } from './ShippingAddressClient';
export declare class ShippingAddressDriver {
    private readonly clientKey;
    private readonly customerKey;
    private readonly gtid;
    private readonly redirectUrl?;
    widget: ShippingAddressWidget | null;
    client: ShippingAddressClient | null;
    private config;
    constructor(clientKey: string, customerKey: string, gtid: string, redirectUrl?: string | undefined);
    render(selector: string): import("./ShippingAddressWidget").PublicShippingAddressWidget | null;
    isWidgetUsed(): boolean;
    addShippingAddressForCafe24Landing(): Promise<import("@payment-widget/models").PublicShippingAddress>;
    private listenOpenShippingAddressClient;
    private listenBrandpayRegister;
    listenBrandpayPaymentRequest(iframe: HTMLIFrameElement): void;
}
