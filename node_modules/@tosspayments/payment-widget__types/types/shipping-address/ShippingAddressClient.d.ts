import { PublicShippingAddress, Config } from '@payment-widget/models';
export declare class ShippingAddressClient {
    private readonly config;
    private readonly redirectUrl?;
    private element;
    constructor(config: Config, redirectUrl?: string | undefined);
    open({ origin, isDimmer }?: {
        origin?: string;
        isDimmer?: boolean;
    }): void;
    close(): void;
    openForCafe24Landing(): Promise<PublicShippingAddress>;
    private createClientIframe;
    private connectBridge;
}
