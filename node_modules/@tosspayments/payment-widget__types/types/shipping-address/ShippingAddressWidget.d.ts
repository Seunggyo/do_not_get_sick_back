import { PublicShippingAddress, ShippingAddress, Config } from '@payment-widget/models';
import { WidgetIframe } from '../widget-iframe/WidgetIframe';
export declare class ShippingAddressWidget {
    private readonly config;
    private readonly redirectUrl?;
    private selector;
    private iframe;
    private selectedShippingAddress;
    publicWidget: PublicShippingAddressWidget | null;
    constructor(config: Config, redirectUrl?: string | undefined);
    render(selector: string): WidgetIframe;
    rerender(): void;
    validate(): void;
    handleError(error: unknown): void;
    saveShippingAddress(): Promise<unknown>;
    saveShippingAddressListToServer(): Promise<unknown>;
    private createShippingAddressWidgetIframe;
    private connectBridge;
}
export declare class PublicShippingAddressWidget {
    onSelectHandler: ((address: PublicShippingAddress) => void) | null;
    on(event: 'select', callback: (address: PublicShippingAddress) => void): void;
    handleSelect(value: ShippingAddress): void;
}
