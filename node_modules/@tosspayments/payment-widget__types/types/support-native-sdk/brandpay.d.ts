import { BrandpayDriver } from '../addon-payment-method/driver/brandpayDriver';
import { BrandpayConfig } from '../utils/api/brandpay.api';
export declare function createBrandpayOpenSettings({ clientKey, customerKey, redirectUrl, ui, }: {
    clientKey: string;
    customerKey: string;
    redirectUrl?: string;
    ui?: BrandpayConfig['ui'];
}): Promise<void>;
export declare function createBrandpayRequestPayment({ clientKey, customerKey, redirectUrl, ui, brandpayPaymentParams, }: {
    clientKey: string;
    customerKey: string;
    redirectUrl?: string;
    ui?: BrandpayConfig['ui'];
    brandpayPaymentParams: ReturnType<InstanceType<typeof BrandpayDriver>['getRequestPaymentParams']>;
}): string;
