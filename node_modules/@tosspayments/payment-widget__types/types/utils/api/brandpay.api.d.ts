export interface BrandpayConfig {
    clientKey: string;
    ui: {
        highlightColor: string;
    };
}
export declare function fetchBrandpayConfig(widgetKey: string, variantKey: string): Promise<{
    clientKey: string;
    ui: {
        highlightColor: string;
    };
} | null>;
export declare function fetchBrandpayCustomerToken(clientKey: string, customerKey: string, redirectUrl?: string): Promise<string>;
