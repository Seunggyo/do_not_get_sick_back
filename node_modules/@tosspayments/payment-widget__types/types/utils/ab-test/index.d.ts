/**
 * NOTE(zih0): customerKey를 기준으로 A/B 테스트를 진행합니다.
 * CDN 캐싱 정책으로 인해 iframe의 쿼리파라미터로 customerKey를 넘길 수 없어서 SDK 단에서 A/B 분기를 진행합니다.
 */
export declare function decideVariantKey({ widgetKey, customerKey, variantKey: currentVariantKey, }: {
    widgetKey: string;
    customerKey: string;
    variantKey: string;
}): string;
