declare type Ratio = 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 | 9;
declare class ABTestSetting {
    private readonly widgetKeys;
    private readonly targetVariantKey;
    readonly controlRatio: Ratio;
    readonly variantRatio: Ratio;
    readonly ABTestVariantKey: string;
    constructor(widgetKeys: string[], targetVariantKey: string, controlRatio: Ratio, variantRatio: Ratio, ABTestVariantKey: string);
    isTarget({ widgetKey, variantKey }: {
        widgetKey: string;
        variantKey: string;
    }): boolean;
    static of({ widgetKeys, targetVariantKey, controlRatio, variantRatio, ABTestVariantKey, }: {
        widgetKeys: string[];
        targetVariantKey: string;
        controlRatio: Ratio;
        variantRatio: Ratio;
        ABTestVariantKey: string;
    }): ABTestSetting;
}
export declare function getTargetABTestSetting({ widgetKey, variantKey, }: {
    widgetKey: string;
    variantKey: string;
}): ABTestSetting | null;
export {};
