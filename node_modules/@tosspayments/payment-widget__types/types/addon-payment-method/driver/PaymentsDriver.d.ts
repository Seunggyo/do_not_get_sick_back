import { TossPaymentsInstance } from '@sdk/payment/src/tosspayments';
import { PaymentRequest, PaymentRequestWithoutUrl } from '@sdk/payment/src/payment/PaymentRequest';
import { WidgetEvent } from '@payment-widget/models';
import { SDKBridge } from '@tosspayments/sdk-bridge';
import { SDKState } from '../../state/SDKState';
import { PaymentRequestParameters } from '../../types/widget';
export declare class PaymentsDriver {
    clientKey: string;
    private paymentSDK;
    constructor(clientKey: string);
    requestPayment(...args: Parameters<TossPaymentsInstance['requestPayment']>): Promise<(import("@sdk/payment/src/payment/requestPayment").RequestPaymentResult & {
        paymentType: "NORMAL";
    }) | undefined>;
    createPaymentParams({ parameters, state, bridge, }: {
        parameters: PaymentRequestParameters;
        state: SDKState;
        bridge: SDKBridge<WidgetEvent>;
    }): Promise<PaymentRequestWithoutUrl | PaymentRequest>;
    private appendPaymentType;
}
