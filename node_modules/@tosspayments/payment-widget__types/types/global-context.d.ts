import { PaymentWidgetOptions } from '@tosspayments/payment-widget-types';
interface BaseGlobalContext {
    gtid: string;
    service?: string;
    environment?: PaymentWidgetOptions['environment'];
}
interface GlobalContext extends BaseGlobalContext {
    [key: string]: unknown;
}
export declare const globalContext: GlobalContext;
export declare function getGlobalTraceIdHeader(gtid?: string): {
    'x-tosspayments-global-trace-id': string;
};
export {};
